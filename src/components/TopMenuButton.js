import React from 'react';

const TopMenuButton = ({text}) => {
    return (
        <div className='tc bg-light-blue'>
            
            <h2>{text}</h2>
        </div>
    )
}

export default TopMenuButton;